#!/usr/bin/env ruby
$LOAD_PATH.push File.join(File.dirname(__FILE__), "/../lib" )
require 'toy_robot_simulator'

simulation = ToyRobotSimulator::Simulation.new(STDOUT)
simulation.start

while command = gets
  simulation.input(command.chomp)
end
